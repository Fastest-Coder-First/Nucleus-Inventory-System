function getCookie(key) {
    let name = key + "=";
    let decodedCookie = decodeURIComponent(document.cookie);
    let ca = decodedCookie.split(';');
    for(let i = 0; i <ca.length; i++) {
      let c = ca[i];
      while (c.charAt(0) == ' ') {
        c = c.substring(1);
      }
      if (c.indexOf(name) == 0) {
        return c.substring(name.length, c.length);
      }
    }
    return "";
}

function setCookie(key, value, exdays) {
    const d = new Date();
    d.setTime(d.getTime() + (exdays*24*60*60*1000));
    let expires = "expires="+ d.toUTCString();
    document.cookie = key + "=" + value + ";" + expires + ";path=/";
}

function deleteCookie(key) {
    document.cookie = key + '=;expires=Thu, 01 Jan 1970 00:00:00 UTC; path=/;';
}

function checkCookie(key) {
    let cookie = getCookie(key);
    if (cookie != "") {
        return true;
    } else {
        return false;
    }
}

// setCookie("admin-id","1bewgr2394",1);
// setCookie("user-id","1bbdoqw3281",1);

deleteCookie("admin-id");

// show cookies
console.log(document.cookie);

if (getCookie("admin-id") == "") {
    // // get total columns of table
    // let totalColumn = $('#storeTable').DataTable().columns.count();
    // console.log(totalColumn);
    // // remove last column from table
    // $('#storeTable').DataTable({
    //     "columnDefs": [
    //         { "visible": false, "targets": totalColumn - 1 }
    //     ]
    // }).draw();
    // write above code for all tables
    $('table').each(function(e) {
        
        $(this).DataTable({
            "columnDefs": [
                { "visible": false, "targets": totalColumn - 1 }
            ]
        });
    });


    console.log("admin-id cookie not found");
}
